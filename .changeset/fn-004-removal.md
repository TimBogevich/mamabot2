---
"mamabot": patch
---

**docs:** Remove `functions/docs/` directory after migrating unique content into `docs/` at repo root. All unique content from `functions/docs/schemas.md`, `functions/docs/deployment.md`, and `functions/docs/i18n.md` has been merged into the canonical `docs/` directory. All project documentation now lives in a single canonical directory (`docs/`).